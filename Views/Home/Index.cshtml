@{
    ViewData["Title"] = "Home Page";
    var mostDiscount = new BookSection("Today's best deals", EnumerableBooks.MostDiscount, 4);
    var mostRating = new BookSection("Greatest of library", EnumerableBooks.MostRating, 4);
    var lessStocks = new BookSection("About to run out", EnumerableBooks.LestStocks, 4);
}

<div>
    <partial name="BookSection/Default" model=@mostDiscount/>
    <partial name="BookSection/Default" model=@mostRating/>
    <partial name="BookSection/Default" model=@lessStocks/>
</div>
