@model muhammedkayraozkaya_241103046.Models.BookContainer
@{
    var className = "book-container";
    if (Model.Count != 0)
        className += " line";
}

<div class="@className">
    @if(Model != null)
    {
        @foreach (var card in Model.Cards)
        {
            <partial name="Book/card" model=@card/>
        }
    }
</div>