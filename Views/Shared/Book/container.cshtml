@model IEnumerable<muhammedkayraozkaya_241103046.Models.BookModel>

<div class="book-container">
    @if(Model != null)
    {
        @foreach (var item in Model)
        {
            <partial name="BookCard/Default" model=@item/>
        }
    }
</div>